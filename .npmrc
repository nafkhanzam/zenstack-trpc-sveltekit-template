# Prevent phantom dependencies
auto-install-peers=false
strict-peer-dependencies=false

# Optimize hoisting for better TypeScript performance
hoist=true
shamefully-hoist=false

# Reduce symlinks for better performance
symlink=true
